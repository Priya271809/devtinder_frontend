#Devtinder

-Created a vite+react project
-Remove unneccessary code and create a hello world app
-Install DaisyUi
-Add navbar component to App.js
-create a navbar.jsx separate component file
-Install react router dom
-Create BrowserRouter > Routes > Route =/Body > RouteChildren
-Create an Outlet in your Body Component
-create a footer 
-Create a login page
-Installl axios
-CORS - install cors in backend => add middleware to with configurations: origin, credentials: true
//=> if you dont pass this then it will no token back to the other API
-whenever you're making API call so pass axios => { withCredentials: true}
-Install redux Toolkit -new toolkit not a old version
=> configureStore => provider => createSlice => add reducer to store
-add redux devTools in chrome
-Login and see if your data is coming in the store
-NavBar should update as soon as user logs in
-Refactor our code to add constants file + create a components folder
-you should not be access other routes without login
-token is not present, redirect user to login page 
-logout feature
-profile


body component
        navbar
        Route=/  =>
        feed
        Route= /login => login
        Route= /connections =>
        Router = /profile => Profile

